configs:
  ceilometer:
    debug: false

    prometheus_port:
      cont: 30000

    auth:
      user: ceilometer
      password: ceilometer

sources:
  openstack/ceilometer:
    git_url: https://git.openstack.org/openstack/ceilometer.git
    git_ref: stable/newton
